/* Hero SVG component removed — kept stub to avoid import errors during incremental changes.
   The component was intentionally removed from templates and imports. */

export const HERO_SVG_REMOVED = true;
