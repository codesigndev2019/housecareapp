name: Accessibility Checks

on: [pull_request]

jobs:
  a11y:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install
        run: |
          npm ci
          npm run build --if-present
      - name: Run axe-core (placeholder)
        run: |
          echo "Run axe checks here (configure cypress-axe or pa11y)"
